<div>
    <label for="title">Title</label>
    <input type="text" name="title" value="<%= locals.event ? event.title : '' %>">
  </div>
  <div>
    <label for="description">Description</label>
    <textarea type="text" name="description"><%= locals.event ? event.description : '' %></textarea>
  </div>
  <input type="submit">